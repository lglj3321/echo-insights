# 不使用数据库进行测试指南

本指南说明如何在不使用任何数据库的情况下进行完整测试。系统会自动使用内存存储（MemStorage）。

## 🚀 快速开始

### 1. 启动开发服务器

```bash
npm run dev
```

**重要**: 不要设置 `DATABASE_URL` 环境变量，这样系统会自动使用内存存储。

### 2. 填充测试数据（内存存储模式）

在**另一个终端**运行：

```bash
npm run test:seed:memory
```

这个脚本会：
- 通过 API 调用创建测试数据
- 创建测试用户（用户名: `testuser`, 密码: `test123`）
- 创建4个测试项目
- 创建调查问题和响应
- 创建目标、团队成员等

### 3. 运行测试

```bash
npm run test:workflow
```

### 4. 访问应用

打开浏览器访问: `http://localhost:5173`

使用测试账户登录：
- **用户名**: `testuser`
- **密码**: `test123`

## 📋 内存存储 vs 数据库存储

### 内存存储（MemStorage）

**优点**:
- ✅ 无需设置数据库
- ✅ 快速启动
- ✅ 适合开发和测试
- ✅ 无需配置

**缺点**:
- ❌ 数据在服务器重启后丢失
- ❌ 不适合生产环境
- ❌ 多实例间数据不共享

### 数据库存储（DatabaseStorage）

**优点**:
- ✅ 数据持久化
- ✅ 适合生产环境
- ✅ 支持多实例

**缺点**:
- ❌ 需要设置数据库
- ❌ 需要配置连接

## 🔄 工作流程

### 使用内存存储的完整流程

1. **启动服务器**（不设置 DATABASE_URL）
   ```bash
   npm run dev
   ```

2. **填充测试数据**
   ```bash
   npm run test:seed:memory
   ```

3. **测试应用**
   - 访问 http://localhost:5173
   - 使用测试账户登录
   - 测试所有功能

4. **运行自动化测试**
   ```bash
   npm run test:workflow
   ```

## ⚠️ 注意事项

### 数据持久性

- **内存存储**: 数据只在服务器运行期间存在
- 服务器重启后，所有数据会丢失
- 每次重启后需要重新运行 `npm run test:seed:memory`

### 多用户测试

- 内存存储在同一服务器实例中共享
- 所有用户看到相同的数据
- 适合单用户或小团队测试

### 性能

- 内存存储通常比数据库存储更快
- 适合快速原型和测试
- 不适合大量数据

## 🔧 切换存储模式

### 从内存存储切换到数据库存储

1. 设置 `DATABASE_URL` 环境变量
2. 重启服务器
3. 使用 `npm run test:seed` 填充数据

### 从数据库存储切换到内存存储

1. 删除或注释掉 `DATABASE_URL` 环境变量
2. 重启服务器
3. 使用 `npm run test:seed:memory` 填充数据

## 📝 测试脚本说明

### `test:seed:memory`

通过 API 调用填充内存存储的测试数据。

**特点**:
- 不需要数据库连接
- 通过 HTTP API 创建数据
- 适用于内存存储模式

### `test:seed`

直接连接数据库填充测试数据。

**特点**:
- 需要 `DATABASE_URL` 环境变量
- 直接操作数据库
- 适用于数据库存储模式

## 🐛 故障排除

### 问题1: 测试数据脚本失败

**错误**: `无法连接到服务器`

**解决方案**:
1. 确保开发服务器正在运行
2. 检查服务器地址（默认: `http://localhost:5173`）
3. 使用 `TEST_BASE_URL` 环境变量指定正确的地址

### 问题2: 数据在重启后丢失

**原因**: 这是内存存储的正常行为

**解决方案**:
- 每次重启后运行 `npm run test:seed:memory`
- 或切换到数据库存储模式

### 问题3: 无法登录

**解决方案**:
1. 确保已运行 `npm run test:seed:memory`
2. 检查服务器是否正常运行
3. 使用正确的测试账户（`testuser` / `test123`）

## 🎯 使用场景

### 适合使用内存存储的场景

- ✅ 快速原型开发
- ✅ 功能测试
- ✅ 演示和展示
- ✅ 学习和实验
- ✅ 不需要数据持久化的场景

### 不适合使用内存存储的场景

- ❌ 生产环境
- ❌ 需要数据持久化
- ❌ 多服务器实例
- ❌ 大量数据存储

## 📚 相关文档

- `TEST_DATA_GUIDE.md` - 测试数据指南（数据库模式）
- `TEST_WORKFLOW_GUIDE.md` - 测试工作流指南
- `QUICK_START.md` - 快速开始指南

## 🔗 相关命令

```bash
# 内存存储模式
npm run dev                    # 启动服务器（不设置 DATABASE_URL）
npm run test:seed:memory       # 填充测试数据
npm run test:workflow          # 运行测试

# 数据库存储模式
npm run setup:env              # 设置数据库连接
npm run test:seed              # 填充测试数据（需要数据库）
npm run test:workflow          # 运行测试
```

## ✅ 总结

使用内存存储进行测试非常简单：

1. **不设置** `DATABASE_URL` 环境变量
2. 启动服务器: `npm run dev`
3. 填充数据: `npm run test:seed:memory`
4. 开始测试！

无需任何数据库配置，即可进行完整的功能测试！

